# Node / Next.js
node_modules/
.next/
out/
.env
.env.local
.env.*.local

# .NET
bin/
obj/
*.user
appsettings.Development.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Docker
*.log

# Uploaded media files (local file storage) — keep folder via .gitkeep, ignore contents
backend/src/Devliora.WebApi/wwwroot/uploads/*
!backend/src/Devliora.WebApi/wwwroot/uploads/.gitkeep
# Production secrets (preventive, in case ever created locally)
appsettings.Production.json
*.pem
*.key
*.pfx
*.swp
*.swo

# Database dumps / backups — never commit (contain Admins, RefreshTokens, etc.)
*.sql
*.dump
*.dump.gz
backup_*.sql
db-dumps/
# Local dev snapshots from scripts/backup-local.sh (db dumps + uploads tarballs)
backups/
